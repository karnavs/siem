# Used by docker-compose — copy to .env and fill in before `docker compose up`

# Generate with: openssl rand -hex 32
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=

# Optional — leave blank to run AI triage in mock mode
OPENAI_API_KEY=
AI_MODEL=gpt-4o-mini

AWS_REGION=ap-south-1
SNS_ALERT_TOPIC_ARN=
